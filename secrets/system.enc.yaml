# Placeholder: encrypt with `sops --encrypt --in-place secrets/system.enc.yaml`
# once at least one real secret exists. The `.sops.yaml` rules already point
# at this path, so a single SOPS edit lights it up.
placeholder: "set me with sops --encrypt --in-place"
